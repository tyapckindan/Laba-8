public class TowTruck : SpecialTransport
{
    public TowTruck(string name, string model, string number) : base(name, model, number)
    {
    }

    public override void GetInfo()
    {
        base.GetInfo();
    }
}