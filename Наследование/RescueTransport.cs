public class RescueTransport : SpecialTransport
{
    public RescueTransport(string name, string model, string number) : base(name, model, number)
    {
    }
    public override void GetInfo()
    {
        base.GetInfo();
    }
}

